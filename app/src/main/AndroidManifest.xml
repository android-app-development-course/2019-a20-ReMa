<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.iwktd.rema">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--        <activity android:name=".MainActivity"/>-->
        <activity android:name=".PreviewHistory"/>
        <activity android:name=".MyCourseActivity" />
        <activity android:name=".MyCommentActivity" />
        <activity android:name=".PersonalInfo" />
        <activity
            android:name=".PersonalHomePage"
            android:label="@string/title_activity_personal_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LessonList"/>
        <activity android:name=".LessonDetail"/>
        <activity android:name=".SignUpActivity"/>
        <activity android:name=".Search"/>
    </application>

</manifest>
